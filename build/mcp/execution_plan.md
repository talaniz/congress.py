# MCP Execution Plan

## Overview

MCP work is intentionally deferred until the SDK and CLI layers are stable. Future MCP tools should be read-only, thin wrappers around `CongressClient`, and should never duplicate API request logic.

## Placeholder Scope

- Keep MCP planning separate from API and CLI planning.
- Revisit `build/mcp/04_add_mcp_server.md` when SDK bill workflows and CLI basics are complete.
- Do not implement MCP tools until the required SDK methods are tested.

## Future Sessions

- `build/mcp/04_add_mcp_server.md`

## Build Log Update

At the end of each MCP session, append an entry to `build/build_log.md` with the session number and title, goal, completed work, validation, next recommended actions, clarification questions and answers, and fixes made during the session.
